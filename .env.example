# DeskRobo environment variables
# Phase 1 is fully client-side and requires no backend or keys.
# These are placeholders for later phases (analytics sync, optional backend).

# VITE_API_BASE_URL=http://localhost:4000
